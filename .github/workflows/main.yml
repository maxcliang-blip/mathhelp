name: Disabled
# The AoPS Wiki API is behind Cloudflare which blocks GitHub Actions IPs.
# This workflow is disabled. To update page lists, run downloadlists.js
# manually in a browser console on the live site.
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow disabled - Cloudflare blocks API requests from GitHub Actions"
